{
  "name": "@enuesaa/cmdgate-example-books-cli",
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "private": true,
  "files": [
    "dist"
  ],
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "dev": "cd ../../ && pnpm build && cd examples/books-cli && pnpm install && tsx dev.ts",
    "build": "tsx build.ts",
    "test": "vitest --run"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "esbuild": "^0.19.12",
    "ts-node": "^10.9.2",
    "tsx": "^4.7.2",
    "typescript": "^5.4.5",
    "vitest": "^1.5.0"
  },
  "bin": {
    "books": "bin/books.js"
  },
  "dependencies": {
    "@enuesaa/cmdgate": "file:../.."
  }
}
